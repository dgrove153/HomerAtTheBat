extends layout

block content
	if user !== null
		h1 I'm Embarrassed Most By #{user.firstName}
		a(href="/logout") Logout
		br
		a(href="/profile") Profile
	else
		h1 Homer At The Bat
		br
		a(href="/login") Login
		br
		a(href="/signup") SignUp
